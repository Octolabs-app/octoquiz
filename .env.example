# OctoQuiz environment variables
# No runtime environment variables are required for the public MVP.
#
# Production realtime runs through the Cloudflare Worker at /realtime/:room,
# backed by a Durable Object per room code.
